# scratchai
make an ai app completely from scratch no dependencies also I want you to even make the API the backend the front the transformers models all from scratch the nn the gnn the CNN the chat bots the agents the uis everything about it make the app 
